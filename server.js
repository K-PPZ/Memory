// import express from "express";
// const app = express();
// const port = 5173;

// app.use(express.static(process.cwd()));

// app.get("/", (req, res) => {
//     res.sendFile(`${process.cwd()}/index.html`);
// });

// app.listen(port);